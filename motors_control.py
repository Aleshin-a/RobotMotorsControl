"""
Скрипт для управления двигателями робота.
"""


def move_forward(speed, duration):
    """
    Движение вперед с заданной скоростью в течение заданного времени.
    """
    print(f"Движение вперед со скоростью {speed} на {duration} секунд.")
    # Здесь должен быть код для управления моторами робота
    # Например, отправка команд на микроконтроллер


def stop():
    """
    Остановка двигателей.
    """
    print("Остановка двигателей.")
    # Здесь должен быть код для остановки моторов


if __name__ == "__main__":
    move_forward(50, 2) # Движение вперед со скоростью 50 на 2 секунды
    stop()
"""
Скрипт для управления двигателями робота.
"""


import time


def set_speed(speed):
    """
    Устанавливает скорость двигателей.
    """
    print(f"Установка скорости: {speed}")
    # Код для установки скорости моторов (например, отправка команд)


def move_forward(speed, duration):
    """
    Движение вперед с заданной скоростью в течение заданного времени с плавным изменением скорости.
    """
    print(f"Движение вперед со скоростью {speed} на {duration} секунд (плавное изменение).")


    # Плавное увеличение скорости
    for i in range(0, speed, 10):
        set_speed(i)
        time.sleep(0.1)  # Небольшая задержка


    set_speed(speed) # Установка целевой скорости


    time.sleep(duration)


    # Плавное уменьшение скорости
    for i in range(speed, 0, -10):
        set_speed(i)
        time.sleep(0.1)


def stop():
    """
    Остановка двигателей.
    """
    print("Остановка двигателей.")
    set_speed(0) # Устанавливаем скорость в 0 перед остановкой


if __name__ == "__main__":
    move_forward(100, 3)  # Движение вперед с плавной скоростью 100 на 3 секунды
    stop()
